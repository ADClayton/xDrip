<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="com.eveningoutpost.dexdrip.permissions.RECEIVE_BG_ESTIMATE" />

    <application android:allowBackup="true" android:icon="@mipmap/appicon" android:label="Glucose Broadcast Viewer">
        <receiver android:name=".Platforms.Android.Receivers.GlucoseBroadcastReceiver"
                  android:enabled="true"
                  android:exported="true"
                  android:permission="com.eveningoutpost.dexdrip.permissions.RECEIVE_BG_ESTIMATE">
            <intent-filter>
                <action android:name="com.eveningoutpost.dexdrip.BgEstimate" />
                <action android:name="com.eveningoutpost.dexdrip.StatusUpdate" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
